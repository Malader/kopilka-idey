# Копилка идей: форма сбора идей

Публичная страница с формой. Заявки падают строками в Google Таблицу, ответственным уходит письмо и сообщение в телеграм, автору идеи приходит автоответ с номером заявки. Хостинг бесплатный, платный будет только домен.

**Всё настроено и работает.**

| Что | Где |
| --- | --- |
| Форма (боевая) | <https://malader.github.io/kopilka-idey/> |
| Репозиторий | <https://github.com/Malader/kopilka-idey> |
| Таблица с заявками | <https://docs.google.com/spreadsheets/d/1zn2x0DWamtTg4sb29s_pEo51rCpAYG4H41YMBp_RI2A/edit> |
| Скрипт-приёмник | <https://script.google.com/home/projects/1rA257pieegg7jrYuvXrGb3HrfF6FE5uhXKdNkA-GMgBsJ8pUPmbbEkvk/edit> |
| Аккаунт, на котором всё живёт | v.malyshev@g.nsu.ru |
| Куда приходят письма о заявках | v.malyshev@g.nsu.ru |
| Телеграм | код готов, ждёт токен бота (см. ниже) |

## Что внутри репозитория

```
kopilka-idey/
├── index.html          вся страница одним файлом (вёрстка, стили, логика)
├── .nojekyll           отключает лишнюю обработку на стороне GitHub Pages
├── README.md           этот файл
└── apps-script/
    └── Code.gs         приёмник заявок, копия того, что стоит в таблице
```

Отдельного сервера и базы нет: страница статическая, роль бэкенда выполняет скрипт внутри Google Таблицы.

## Как это работает

1. Сотрудник заполняет форму на GitHub Pages.
2. Браузер отправляет данные на веб-приложение Apps Script (адрес прописан в `index.html`, переменная `SCRIPT_URL`).
3. Скрипт дописывает строку на лист «Заявки», отправляет письмо ответственным, сообщение в телеграм и автоответ автору. Любое из уведомлений может отвалиться, заявка от этого не потеряется: она уже в таблице.
4. Если основной путь отправки не проходит (блокировки, обрыв связи), срабатывает запасной. Чтобы заявка при этом не записалась дважды, клиент шлёт одноразовый номер отправки `submissionId`, а скрипт отбрасывает повтор.

---

## Что делать дальше

### Подключить домен (когда заказчик его выберет)

1. Купить домен (`.ru` стоит примерно 200–900 ₽ в год у любого регистратора).
2. В DNS домена добавить записи:
   - для домена вида `ideas.company.ru`: одна запись **CNAME** со значением `malader.github.io`;
   - для домена без поддомена вида `company.ru`: четыре записи **A** на адреса `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`.
3. В репозитории: **Settings → Pages → Custom domain**, вписать домен и сохранить. GitHub сам создаст файл `CNAME`, сертификат HTTPS выпустится автоматически за 10–60 минут.
4. Поставить галочку **Enforce HTTPS**.

Хостинг при этом остаётся бесплатным.

### Включить телеграм (5 минут, делается один раз)

Код уже стоит и ждёт токен. Токен бота это пароль от бота, поэтому вставляете его вы, а не я.

1. В телеграме напишите **@BotFather** команду `/newbot`, придумайте боту имя и адрес. В ответ придёт строка вида `1234567890:AAE...` это и есть токен.
2. Откройте [скрипт](https://script.google.com/home/projects/1rA257pieegg7jrYuvXrGb3HrfF6FE5uhXKdNkA-GMgBsJ8pUPmbbEkvk/edit) → **Настройки проекта** → **Свойства скрипта** → **Изменить свойства скрипта**. В строке `TELEGRAM_TOKEN` замените заглушку `vstavte-syuda-token-ot-BotFather` на токен и сохраните.
3. Решите, куда должны падать заявки:
   - **лично вам**: просто напишите своему боту любое сообщение;
   - **в общий чат команды**: создайте группу, добавьте туда бота и напишите в группе любое сообщение.
4. Вернитесь в **Редактор**, выберите в списке функций `podklyuchitTelegram` и нажмите **Выполнить**. Скрипт сам найдёт чат, запомнит его и пришлёт туда проверочное сообщение.

Всё, следующая заявка придёт и на почту, и в телеграм. Новое развертывание для этого делать не нужно, свойства скрипта подхватываются сразу.

Выключить телеграм обратно: функция `otklyuchitTelegram`, письма при этом продолжат приходить.

### Добавить адреса заказчика в рассылку

В таблице: **Расширения → Apps Script**, в блоке `CONFIG` строка `NOTIFY_EMAILS`, адреса через запятую. Затем обязательно **Начать развертывание → Управление развертываниями → карандаш → Версия: Новая версия → Развернуть**. Без этого правка не применится. Адрес веб-приложения при таком обновлении не меняется, `index.html` трогать не надо.

### Выдать доступ к заявкам коллегам

В таблице кнопка **Настройки доступа**, добавить адреса. Выгрузка в Excel: **Файл → Скачать → Microsoft Excel (.xlsx)**.

### Поменять вёрстку формы

Правим `index.html`, коммитим, пушим в `main`. GitHub Pages пересобирает страницу за 1–3 минуты.

---

## Что проверено

- [x] Заявка с живой страницы доезжает до таблицы, поля на своих местах, кириллица не бьётся.
- [x] Письмо ответственным приходит, кнопка «Открыть таблицу заявок» ведёт куда надо.
- [x] Автору приходит автоответ с номером заявки.
- [x] Повторная отправка с тем же `submissionId` не создаёт вторую строку и не шлёт второе письмо.
- [x] Пустая форма не отправляется, незаполненные поля подсвечиваются красным (12 полей).
- [x] Почта без собачки отклоняется.
- [x] Без галочки согласия на обработку ПД отправка не проходит.
- [x] Скрытое поле-ловушка и слишком быстрое заполнение отсекаются, строка не пишется.
- [x] Мобильная вёрстка: горизонтального скролла нет, кнопка во всю ширину, блоки в один столбец.
- [x] Эндпоинт доступен без входа в Google, то есть форму заполнит любой человек из интернета.
- [x] Пока телеграм не настроен, отправка в него молча пропускается и ничего не ломает.
- [ ] Сообщение в телеграм в бою: проверяется после того, как вставите токен.

## Что стоит знать

**Лимиты Google.** Аккаунт Workspace (g.nsu.ru): до 1500 писем в сутки со скрипта. Для формы идей этого с большим запасом хватает. Строк в таблице можно до 10 миллионов ячеек, то есть десятки тысяч заявок.

**Админка.** Отдельной админки нет и пока не нужна: сама таблица её заменяет. Есть колонки «Статус» и «Комментарий» для ручной разметки, фильтры и поиск встроенные, выгрузка в Excel в два клика.

**Файлы.** Загрузка файлов на статическом хостинге невозможна, поэтому поле 13 просит ссылку на файл в облаке. Настоящие вложения появятся, когда будет полноценный бэкенд.

**Спам.** Стоит скрытое поле-ловушка и проверка на слишком быстрое заполнение. Для публичной формы обычно достаточно. Если начнёт лететь мусор, следующий шаг, капча (Yandex SmartCaptcha, бесплатна).

**Персональные данные.** Форма собирает ФИО и почту, то есть персональные данные, и хранит их на серверах Google за пределами РФ. Формально по 152-ФЗ база с ПД россиян должна находиться в России. Для внутреннего пилота на это обычно закрывают глаза, но заказчика лучше предупредить письменно. Если требование жёсткое, схема та же по сути, но данные складываются в Яндекс или на российский хостинг, а вёрстку переделывать не придётся. В форме уже есть галочка согласия на обработку ПД, при необходимости к ней прикручивается ссылка на политику.

**Аккаунт скрипта.** Скрипт запускается от имени v.malyshev@g.nsu.ru и имеет права на таблицы этого аккаунта и на отправку писем от его имени. Отозвать можно в настройках аккаунта Google, раздел «Сторонние приложения с доступом к аккаунту».

**Телеграм.** Токен бота лежит в свойствах скрипта, а не в коде, потому что репозиторий публичный. Если токен утечёт, его отзывают в @BotFather командой `/revoke`. Лимит телеграма на сообщения от бота с большим запасом перекрывает поток заявок.

**Переход на свой бэкенд.** Меняется только адрес в `SCRIPT_URL`. Страница, вёрстка и набор полей остаются прежними.
